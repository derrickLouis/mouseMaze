import React from 'react';
import ReactDOM from 'react-dom/client';
import StrategicMiceGame from './MouseMaze.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <StrategicMiceGame />
  </React.StrictMode>
);
